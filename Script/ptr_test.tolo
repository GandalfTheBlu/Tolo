#include <memory>
#include <io>
#include "Script/include_me.tolo"
#include "Script/include_me_2.tolo"

void main()
{
	// from <memory>
	ptr stack_str = "str\n";
	int len = strlen(stack_str);
	ptr heap_str = malloc(len + 1);
	memcpy(heap_str, stack_str, len + 1);

	// from <io>
	print(heap_str);

	// from "include_me"
	vec3 v = vec3(1., 2., 3.);
	vec3 u = vec3(1., 1., 1.);
	vec3 w = v + u;

	if(w.x == 2.)
	{
		print("yay!\n");
	}

	// from "include_me_2"
	myFun("hello\n");
}