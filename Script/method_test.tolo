#include <memory>
#include <io>

struct Animal
{
    int age;
};

int Animal::GetAge()
{
    return this.age;
}

struct Dog : Animal
{};

struct Cat : Animal
{
    int lives;
};

void main()
{
    Dog dog = Dog(12);
    int age1 = dog.GetAge();
    print(age1);
    print("\n");

    Animal::ptr pDog = Animal::ptr(&dog);
    int age2 = pDog.GetAge();
    print(age2);
    print("\n");

    Cat cat = Cat(2, 9);
    print(cat.lives);
}