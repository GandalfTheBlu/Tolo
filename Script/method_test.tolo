#include <io>

struct thing
{
    int a;
};

int thing::func(int val)
{
    this.a = this.a + val;
    return this.a;
}

struct Obj
{
    int a;
    int b;
    thing::ptr p;
};

void print_memb(test_struct::ptr p)
{
    print_int(p.memb);
}

void main()
{
    thing t = thing(33);
    Obj o = Obj(11, 22, thing::ptr(&t));

    int a = o.p.func(11);

    print_int(a);
    print_str("\n");
    print_int(sizeof(Obj));
    print_str("\n");

    test_struct ts = test_struct(1337);

    print_memb(test_struct::ptr(&ts));
}