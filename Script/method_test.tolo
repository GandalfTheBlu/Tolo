#include <io>

struct thing
{
    int a;
};

int thing::func(int val)
{
    this.a = this.a + val;
    return this.a;
}

struct Obj
{
    int a;
    int b;
    thing::ptr p;
};

void main()
{
    thing t = thing(33);
    Obj o = Obj(11, 22, thing::ptr(&t));

    int a = o.p.func(11);

    print_int(a);
}